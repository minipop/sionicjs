{
  "name": "sionicjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:js": "browserify index.js --standalone Sionic > dist/sionic.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:js": "watchify index.js --standalone Sionic -o dist/sionic.js -dv"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^13.0.0",
    "derequire": "^2.0.3",
    "watchify": "^3.7.0"
  }
}
